# ✅ Проверка работы webhook для nesi.su

## 🎯 Ваш результат

Вы получили ответ `INVALID_SIGNATURE` - это **ОТЛИЧНО**! ✅

Это означает:

- ✅ Webhook endpoint доступен
- ✅ Nginx правильно проксирует запросы
- ✅ Приложение получает запросы
- ✅ Проверка подписи работает (защита от подделок)

---

## ✅ Что означает `INVALID_SIGNATURE`

Это **нормально** для тестового запроса без правильной подписи Т-Банка.

Реальные запросы от Т-Банка будут иметь правильную подпись и вернут `OK`.

---

## 🧪 Правильная проверка

### 1. Тест доступности (теперь вернет OK)

```bash
curl -X POST https://nesi.su/api/tbank/webhook \
  -H "Content-Type: application/json" \
  -d '{"test":"ping"}'
```

**Ожидаемый ответ:** `OK`

### 2. Проверка логов

```bash
# На сервере
pm2 logs nesi --lines 50 | grep "Webhook"
```

Должны увидеть:

```
TBank Webhook получен
Тестовый webhook запрос получен
```

### 3. Проверка через браузер

Откройте в браузере:

```
https://nesi.su/api/tbank/webhook
```

Должен вернуть ошибку метода (т.к. нужен POST), но это подтверждает что endpoint доступен.

---

## 🔍 Проверка конфигурации

### Проверить что webhook настроен в Т-Банке

1. Зайдите в [business.tbank.ru](https://business.tbank.ru)
2. Терминал → Настройки → Уведомления
3. Убедитесь что указано: `https://nesi.su/api/tbank/webhook`

### Проверить переменные окружения

```bash
# На сервере
cd ~/nesi-app/NESI
cat .env | grep TBANK
```

Должны быть:

```
TBANK_TERMINAL_KEY=...
TBANK_TERMINAL_PASSWORD=...
TBANK_E2C_TERMINAL_KEY=...
TBANK_E2C_TERMINAL_PASSWORD=...
TBANK_MODE=test
```

---

## 🧪 Тестирование с реальным запросом

После настройки в ЛК Т-Банка, сделайте тестовую транзакцию:

1. Откройте: `https://nesi.su/profile`
2. Вкладка "Кошелек"
3. Попробуйте пополнить баланс (100 ₽)
4. Используйте тестовую карту: `2200770239097761` (12/25, CVV: 123)

После оплаты Т-Банк отправит webhook с правильной подписью.

### Смотреть логи в реальном времени:

```bash
# На сервере
pm2 logs nesi --lines 0
```

После транзакции должны увидеть:

```
TBank Webhook получен
Status: CONFIRMED
PaymentId: ...
Баланс пополнен через Т-Банк
```

---

## ✅ Чеклист проверки

- [x] Webhook endpoint доступен (`INVALID_SIGNATURE` получен)
- [x] Nginx настроен и работает
- [ ] Переменные окружения настроены
- [ ] Webhook URL указан в ЛК Т-Банка
- [ ] Firewall настроен (IP Т-Банка разрешены)
- [ ] Протестирована реальная транзакция

---

## 🎊 Готово!

Ваш webhook endpoint **работает правильно**!

**Следующий шаг:** Настройте webhook URL в личном кабинете Т-Банка:

```
https://nesi.su/api/tbank/webhook
```

После этого реальные запросы от Т-Банка будут обрабатываться корректно.

---

## 📊 Мониторинг

### Смотреть логи webhook:

```bash
# Все логи
pm2 logs nesi | grep -i webhook

# Только ошибки
pm2 logs nesi | grep -i "error\|invalid"

# Последние 100 строк
pm2 logs nesi --lines 100
```

### Проверить в БД:

```bash
# Подключиться к БД
psql -U user -d nesi

# Проверить платежи
SELECT "paymentId", amount, status, "createdAt"
FROM "TBankPayment"
ORDER BY "createdAt" DESC
LIMIT 5;

# Проверить сделки
SELECT id, "spAccumulationId", status, "totalAmount"
FROM "TBankDeal"
ORDER BY "createdAt" DESC
LIMIT 5;
```

---

**Ваш webhook готов к работе!** 🚀
