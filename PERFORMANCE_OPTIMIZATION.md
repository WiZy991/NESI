# ⚡ ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ (PageSpeed)

## 🔍 АУДИТ ПРОИЗВОДИТЕЛЬНОСТИ

### Текущие проблемы:

#### 1. Изображения
- **Проблема:** SVG без оптимизации, нет WebP
- **Файлы:** 
  - `src/app/page.tsx` — `/nesi_snake.svg`, `/anime_images.svg`
  - Все страницы с Image компонентами
- **Исправление:**
  ```tsx
  // ❌ БЫЛО:
  <Image src="/nesi_snake.svg" alt="" width={400} height={600} />
  
  // ✅ ДОЛЖНО БЫТЬ:
  <Image 
    src="/nesi_snake.webp" 
    alt="Декоративное изображение платформы NESI"
    width={400} 
    height={600}
    sizes="(max-width: 768px) 100vw, 50vw"
    priority={false}
    loading="lazy"
  />
  ```

#### 2. JavaScript Bundle
- **Проблема:** Большие клиентские компоненты
- **Файлы:**
  - `src/app/tasks/TaskCatalogPage.tsx` (1157 строк)
  - `src/app/specialists/page.tsx` (614 строк)
- **Исправление:**
  ```tsx
  // Разделить на компоненты:
  // - TaskFilters.tsx
  // - TaskList.tsx
  // - TaskSearch.tsx
  // Использовать dynamic imports:
  const TaskFilters = dynamic(() => import('./TaskFilters'), { ssr: false })
  ```

#### 3. SSR/CSR Архитектура
- **Проблема:** Ключевые страницы полностью клиентские
- **Исправление:**
  ```tsx
  // ✅ ПРАВИЛЬНАЯ СТРУКТУРА:
  // page.tsx (Server Component)
  export const metadata = { ... }
  
  export default async function Page() {
    return (
      <>
        <h1>Заголовок</h1>
        <SEOText />
        <Suspense fallback={<Skeleton />}>
          <ClientCatalog />
        </Suspense>
      </>
    )
  }
  ```

#### 4. Font Loading
- **Статус:** ✅ Уже оптимизировано через next/font
- **Рекомендация:** Проверить preload для критических шрифтов

#### 5. CSS Optimization
- **Проблема:** Большой bundle CSS
- **Исправление:**
  - Использовать CSS Modules
  - Удалить неиспользуемые стили
  - Минифицировать в production

---

## 🎯 РЕКОМЕНДАЦИИ ПО АРХИТЕКТУРЕ

### Что вынести в Server Components:

1. **Мета-теги** — ✅ уже в layout
2. **H1 и SEO-тексты** — вынести в Server Component
3. **Статический контент** — FAQ, преимущества
4. **Breadcrumbs** — Server Component

### Что оставить в Client Components:

1. **Интерактивные фильтры** — поиск, сортировка
2. **Динамические списки** — задачи, специалисты
3. **Модальные окна** — формы, подтверждения
4. **Анимации** — framer-motion компоненты

---

## 📦 CODE SPLITTING

### Рекомендуемые изменения:

```tsx
// 1. Lazy load тяжелых компонентов
const TaskFilters = dynamic(() => import('./TaskFilters'), {
  loading: () => <FiltersSkeleton />,
  ssr: false
})

// 2. Разделить bundle
const HeavyChart = dynamic(() => import('./HeavyChart'), {
  ssr: false
})

// 3. Prefetch для важных страниц
<Link href="/tasks" prefetch={true}>Каталог задач</Link>
```

---

## 🖼️ ОПТИМИЗАЦИЯ ИЗОБРАЖЕНИЙ

### План действий:

1. **Конвертировать SVG в WebP:**
   ```bash
   # Использовать sharp или imagemin
   npx imagemin public/*.svg --out-dir=public/optimized --plugin=svgo
   ```

2. **Добавить responsive images:**
   ```tsx
   <Image
     src="/image.webp"
     width={1200}
     height={630}
     sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
     alt="Описание"
   />
   ```

3. **Использовать priority для above-the-fold:**
   ```tsx
   <Image priority src="/hero.webp" ... />
   ```

---

## ⚡ CORE WEB VITALS

### Цели:

- **LCP (Largest Contentful Paint):** < 2.5s
- **FID (First Input Delay):** < 100ms
- **CLS (Cumulative Layout Shift):** < 0.1

### Исправления:

1. **LCP:**
   - Оптимизировать hero изображения
   - Preload критических ресурсов
   - Минимизировать render-blocking CSS

2. **FID:**
   - Code splitting
   - Lazy load не критичных скриптов
   - Оптимизировать JavaScript

3. **CLS:**
   - Указать размеры изображений
   - Избегать динамических вставок контента
   - Использовать skeleton loaders

---

## 🔧 КОНКРЕТНЫЕ ИСПРАВЛЕНИЯ

### Файл: src/app/page.tsx

```diff
- <Image src="/nesi_snake.svg" alt="" ... />
+ <Image src="/nesi_snake.webp" alt="Декоративное изображение платформы NESI" ... />

- <Image src="/anime_images.svg" alt="..." ... />
+ <Image src="/anime_images.webp" alt="..." sizes="(max-width: 768px) 100vw, 50vw" ... />
```

### Файл: src/app/tasks/page.tsx

```diff
+ import type { Metadata } from 'next'
+ 
+ export const metadata: Metadata = {
+   title: '...',
+   description: '...',
+ }
+ 
  export default function Page() {
    return (
    <>
+     <h1>Каталог фриланс задач</h1>
+     <SEOText />
      <Suspense fallback={<TasksPageSkeleton />}>
        <TaskCatalogPage />
      </Suspense>
    </>
    )
  }
```

---

## 📊 МЕТРИКИ ДО И ПОСЛЕ

### До оптимизации (оценка):
- PageSpeed: 65-75
- LCP: 3.5s
- FID: 150ms
- CLS: 0.15

### После оптимизации (цель):
- PageSpeed: 90+
- LCP: < 2.0s
- FID: < 50ms
- CLS: < 0.05

---

## ✅ ЧЕКЛИСТ ОПТИМИЗАЦИИ

- [ ] Конвертировать все изображения в WebP
- [ ] Добавить sizes для всех Image компонентов
- [ ] Вынести SEO-контент в Server Components
- [ ] Разделить большие компоненты на мелкие
- [ ] Использовать dynamic imports для тяжелых компонентов
- [ ] Оптимизировать CSS (удалить неиспользуемые стили)
- [ ] Добавить preload для критических ресурсов
- [ ] Настроить prefetch для важных страниц
- [ ] Проверить Core Web Vitals
- [ ] Протестировать на мобильных устройствах

