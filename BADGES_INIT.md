# 🏅 Инициализация бейджей на продакшене

## Проблема
Если достижения не работают на боевом сервере, значит бейджи не созданы в базе данных.

## Решение

### Способ 1: Через API (самый простой)
1. Войдите в админ-панель
2. Откройте консоль браузера (F12)
3. Выполните:
```javascript
fetch('/api/admin/badges/seed', { method: 'POST', headers: { 'Authorization': 'Bearer YOUR_TOKEN' } })
  .then(r => r.json())
  .then(console.log)
```

### Способ 2: Через SSH на сервере
```bash
cd /path/to/your/app
npx prisma db seed
```

### Способ 3: Автоматическая инициализация
Бейджи должны автоматически создаваться при первом запуске приложения через `ensureDatabaseSchema`, но если это не произошло, используйте способы 1 или 2.

## Проверка
После инициализации проверьте:
```sql
SELECT COUNT(*) FROM "Badge";
```
Должно быть 20 бейджей.

