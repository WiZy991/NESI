# 🚨 БЫСТРОЕ РЕШЕНИЕ: Пополнение и вывод не работают

## ❌ Текущие проблемы:

1. **Ошибка 500 при пополнении баланса**
2. **Ошибка "Не найден DealId" при выводе**

---

## ⚡ Экспресс-решение (5 минут)

### Шаг 1: Проверьте переменные окружения

**На сервере выполните:**

```bash
# Проверка наличия ключей Т-Банка
grep TBANK .env
# или
cat .env | grep TBANK
```

**Должны быть:**

```
TBANK_TERMINAL_KEY="ваш_terminal_key"
TBANK_PASSWORD="ваш_password"
TBANK_E2C_TERMINAL_KEY="ваш_e2c_key"  # опционально
```

**Если нет - добавьте:**

```bash
nano .env
# Добавьте строки:
TBANK_TERMINAL_KEY="ваш_ключ_от_тбанка"
TBANK_PASSWORD="ваш_пароль_от_тбанка"
```

**Сохраните (Ctrl+O, Enter, Ctrl+X) и перезапустите:**

```bash
pm2 restart all --update-env
```

---

### Шаг 2: Примените миграцию БД

**Выполните SQL:**

```bash
cd NESI

# Если есть psql
psql $DATABASE_URL -f apply_dealid_migration.sql

# Или через Prisma
npx prisma migrate deploy
```

**Или вручную подключитесь к БД и выполните:**

```sql
ALTER TABLE "Transaction" ADD COLUMN IF NOT EXISTS "dealId" TEXT;
ALTER TABLE "Transaction" ADD COLUMN IF NOT EXISTS "paymentId" TEXT;
CREATE INDEX IF NOT EXISTS "Transaction_dealId_idx" ON "Transaction"("dealId");
CREATE INDEX IF NOT EXISTS "Transaction_paymentId_idx" ON "Transaction"("paymentId");
```

---

### Шаг 3: Настройте вебхук в Т-Банке

1. **Откройте:** https://business.tbank.ru/
2. **Перейдите:** Интернет-эквайринг → Настройки терминала
3. **Найдите:** Раздел "Вебхуки" или "Уведомления"
4. **Укажите URL:**
   ```
   https://nesi.su/api/wallet/tbank/webhook
   ```
5. **Убедитесь:** Вебхук активен (включен)

---

### Шаг 4: Проверьте доступность вебхука

```bash
curl https://nesi.su/api/wallet/tbank/webhook
```

**Должен вернуть:**

```json
{
  "status": "ok",
  "message": "Webhook is available...",
  ...
}
```

**Если ошибка:**

- Проверьте, что приложение запущено
- Проверьте файрвол/nginx

---

## 🔍 Диагностика

### Тест 1: Проверка переменных окружения

```bash
cd NESI
node -e "require('dotenv').config(); console.log('TBANK_TERMINAL_KEY:', process.env.TBANK_TERMINAL_KEY ? 'OK' : 'MISSING'); console.log('TBANK_PASSWORD:', process.env.TBANK_PASSWORD ? 'OK' : 'MISSING');"
```

**Ожидается:**

```
TBANK_TERMINAL_KEY: OK
TBANK_PASSWORD: OK
```

### Тест 2: Проверка БД

```sql
SELECT column_name
FROM information_schema.columns
WHERE table_name = 'Transaction'
  AND column_name IN ('dealId', 'paymentId');
```

**Ожидается:**

```
 column_name
-------------
 dealId
 paymentId
```

### Тест 3: Проверка логов

```bash
# PM2
pm2 logs --lines 50

# Docker
docker logs your-container --tail 50

# Ищите ошибки типа:
# ❌ TBANK_TERMINAL_KEY не настроен
# ❌ TBANK_PASSWORD не настроен
```

---

## 🎯 Тестирование после исправления

### 1. Тест пополнения:

```bash
# Откройте в браузере:
https://nesi.su/wallet

# Нажмите "Пополнить баланс"
# Введите сумму: 10 ₽
# Должно открыться окно оплаты Т-Банка
```

### 2. Проверка логов после оплаты:

```bash
pm2 logs --lines 100 | grep WEBHOOK
```

**Должно быть:**

```
📥 [WEBHOOK] Входящий запрос от T-Bank
📊 [WEBHOOK] Параметры платежа: { Status: 'CONFIRMED', SpAccumulationId: 123456 }
💰 [WEBHOOK] Начинаем начисление
✅ [WEBHOOK] Начисление успешно: { dealId: '123456' }
```

### 3. Проверка DealId в БД:

```sql
SELECT id, type, amount, dealId, paymentId, createdAt
FROM "Transaction"
WHERE type = 'deposit'
ORDER BY createdAt DESC
LIMIT 1;
```

**Ожидается:**

- `dealId` не NULL
- `paymentId` не NULL

### 4. Тест вывода:

```bash
# Откройте в браузере:
https://nesi.su/wallet

# Нажмите "Вывести средства"
# Введите сумму: 100 ₽
# Должно пройти без ошибки "Не найден DealId"
```

---

## 🆘 Если не помогло

### Проблема: Ошибка 500 остается

**Причина:** Неверные ключи или ошибка подключения к API Т-Банка

**Решение:**

1. Проверьте правильность ключей:

   ```bash
   cat .env | grep TBANK
   ```

2. Проверьте доступ к API Т-Банка:
   ```bash
   curl https://securepay.tinkoff.ru/v2/Init \
     -X POST \
     -H "Content-Type: application/json" \
     -d '{}'
   ```
3. Проверьте логи подробно:
   ```bash
   pm2 logs --lines 200
   ```

### Проблема: DealId все равно не сохраняется

**Причина:** Вебхук не приходит

**Решение:**

1. **Проверьте настройки в Т-Банке:**

   - Вебхук должен быть активен
   - URL должен быть точно `https://nesi.su/api/wallet/tbank/webhook`

2. **Проверьте доступность с внешнего IP:**

   ```bash
   # С другого сервера или через онлайн-сервис
   curl -I https://nesi.su/api/wallet/tbank/webhook
   ```

3. **Проверьте файрвол:**

   ```bash
   # Убедитесь, что порты открыты
   sudo ufw status
   sudo iptables -L
   ```

4. **Добавьте IP Т-Банка в whitelist (если нужно):**
   ```
   185.71.76.0/27
   185.71.77.0/27
   ```

---

## 📋 Чеклист

Отметьте выполненные шаги:

- [ ] Переменные окружения добавлены в `.env`
- [ ] Приложение перезапущено с новыми переменными
- [ ] Миграция БД применена (поля `dealId`, `paymentId` созданы)
- [ ] Вебхук настроен в личном кабинете Т-Банка
- [ ] Вебхук доступен по URL (GET возвращает 200)
- [ ] Тестовое пополнение прошло успешно
- [ ] В логах видно сообщение от вебхука
- [ ] DealId сохранился в БД
- [ ] Тестовый вывод прошел успешно

---

## 📞 Контакты

**Если ничего не помогло:**

1. **Т-Банк поддержка:**

   - Email: acq_help@tbank.ru
   - Спросите: "Почему не приходит SpAccumulationId в вебхуке?"

2. **Проверьте документацию:**
   - [НАСТРОЙКА*ТБАНК*ПОЛНАЯ.md](./НАСТРОЙКА_ТБАНК_ПОЛНАЯ.md)
   - [TBANK_SETUP.md](./TBANK_SETUP.md)

---

**Время решения:** 5-10 минут  
**Последнее обновление:** Ноябрь 2024
