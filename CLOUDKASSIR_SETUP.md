# 📄 Настройка CloudKassir API для формирования кассовых чеков

## 📋 Обзор

Интеграция с API CloudKassir (CloudPayments) для автоматического формирования кассовых чеков при пополнении баланса и других операциях.

## 🔧 Настройка

### 1. Получение учетных данных

1. Зарегистрируйтесь в [CloudPayments](https://cloudpayments.ru/)
2. В личном кабинете перейдите в раздел "Настройки" → "API"
3. Скопируйте:
   - **Public ID** (логин для API)
   - **API Secret** (пароль для API)
   - **ИНН** вашей организации/ИП

### 2. Настройка переменных окружения

Добавьте в файл `.env`:

```env
# CloudKassir API
CLOUDKASSIR_PUBLIC_ID="your_public_id"
CLOUDKASSIR_API_SECRET="your_api_secret"
CLOUDKASSIR_INN="your_inn"

# Настройки чеков (опционально)
CLOUDKASSIR_TAXATION_SYSTEM=1  # 0-ОСН, 1-УСН Доход, 2-УСН Доход-Расход, 3-ЕНВД, 4-ЕСХН, 5-Патент
CLOUDKASSIR_TIMEZONE=2  # Часовая зона (2 = Москва, RTZ 2)
CLOUDKASSIR_AUTO_CREATE_RECEIPTS=true  # Автоматическое создание чеков при пополнении
```

### 3. Настройка webhook в CloudPayments

1. В личном кабинете CloudPayments перейдите в "Настройки" → "Уведомления"
2. Добавьте URL для уведомлений о чеках:
   ```
   https://yourdomain.com/api/wallet/cloudkassir/webhook
   ```
3. Выберите тип уведомления: **Receipt**
4. Сохраните настройки

### 4. Применение миграции БД

Добавлены новые поля в модель `Transaction`:
- `receiptId` - ID чека CloudKassir
- `receiptData` - Данные чека (фискальные реквизиты)

Примените миграцию:

```bash
npx prisma migrate dev --name add_cloudkassir_receipt_fields
npx prisma generate
```

## 🚀 Использование

### Автоматическое создание чеков

При успешном пополнении баланса через Т-Банк чек создается автоматически (если `CLOUDKASSIR_AUTO_CREATE_RECEIPTS=true`).

### Ручное создание чека

```typescript
// POST /api/wallet/cloudkassir/create-receipt
{
  "amount": 1000,
  "transactionId": "transaction_id",
  "email": "user@example.com",  // опционально
  "phone": "+79999999999",      // опционально
  "description": "Пополнение баланса"  // опционально
}
```

### Проверка статуса чека

```typescript
// GET /api/wallet/cloudkassir/receipt-status?receiptId=xxx
```

## 📁 Созданные файлы

| Файл | Описание |
|------|----------|
| `src/lib/cloudkassir.ts` | Основная утилита для работы с API CloudKassir |
| `src/lib/cloudkassir-helper.ts` | Вспомогательные функции для автоматического создания чеков |
| `src/app/api/wallet/cloudkassir/create-receipt/route.ts` | API endpoint для создания чека |
| `src/app/api/wallet/cloudkassir/webhook/route.ts` | Webhook для получения уведомлений о чеках |
| `src/app/api/wallet/cloudkassir/receipt-status/route.ts` | API endpoint для проверки статуса чека |

## 🔐 Безопасность

1. **HMAC подпись**: Webhook проверяет подпись запросов от CloudPayments
2. **Хранение секретов**: API Secret хранится только в переменных окружения
3. **Проверка прав**: Создание чеков доступно только авторизованным пользователям

## 📊 Типы чеков

- **Income** - Приход (при пополнении баланса)
- **IncomeReturn** - Возврат прихода
- **Expense** - Расход
- **ExpenseReturn** - Возврат расхода

## ⚙️ Настройка системы налогообложения

В переменной `CLOUDKASSIR_TAXATION_SYSTEM` укажите код системы налогообложения:

- `0` - Общая система налогообложения (ОСН)
- `1` - Упрощенная система налогообложения (Доход) - **по умолчанию**
- `2` - Упрощенная система налогообложения (Доход минус Расход)
- `3` - Единый налог на вмененный доход (ЕНВД)
- `4` - Единый сельскохозяйственный налог (ЕСХН)
- `5` - Патентная система налогообложения

## 🧪 Тестирование

1. Создайте тестовый платеж через Т-Банк
2. После успешного пополнения проверьте, что чек создан:
   - В логах должно быть сообщение "Чек CloudKassir создан автоматически"
   - В базе данных в транзакции должен быть заполнен `receiptId`
3. Проверьте статус чека через API или в личном кабинете CloudPayments

## 📝 Логирование

Все операции с чеками логируются:
- Создание чека
- Получение уведомлений
- Ошибки создания

Логи можно найти в консоли сервера или в системе логирования.

## ❓ Частые проблемы

### Чек не создается автоматически

1. Проверьте, что `CLOUDKASSIR_AUTO_CREATE_RECEIPTS=true`
2. Проверьте, что все учетные данные указаны правильно
3. Проверьте логи на наличие ошибок

### Ошибка "CloudKassir не настроен"

Убедитесь, что в `.env` указаны все три переменные:
- `CLOUDKASSIR_PUBLIC_ID`
- `CLOUDKASSIR_API_SECRET`
- `CLOUDKASSIR_INN`

### Webhook не приходит

1. Проверьте, что URL webhook указан правильно в личном кабинете CloudPayments
2. Убедитесь, что сервер доступен извне
3. Проверьте firewall и SSL сертификат

## 📚 Дополнительная информация

- [Документация CloudPayments API](https://cloudpayments.ru/docs/api/kassa)
- [Справочник систем налогообложения](https://cloudpayments.ru/docs/api/kassa#taxationsystem)

