# 🔧 Исправление типа параметра SbpMemberId

**Дата:** 20 ноября 2025  
**Проблема:** Ошибка 322 "Неверные параметры" при выводе через СБП (продолжение)

---

## ❌ Проблема

После исправления типа `FinalPayout` (со boolean на строку), ошибка 322 всё ещё возникала.

При дополнительном изучении документации T-Bank обнаружено, что параметр `SbpMemberId` должен передаваться как **число**, а не как **строка**.

**Было (неправильно):**

```typescript
requestBody.SbpMemberId = params.sbpMemberId // ← Строка "100000000004"
```

**В запросе к API:**

```json
{
	"SbpMemberId": "100000000004", // ← JSON строка
	"Token": "..."
}
```

**Проблема:** Согласно официальной документации T-Bank (multisplit.md стр. 1083), параметр `SbpMemberId` должен быть типа **Number**, а не String.

---

## 📝 Согласно документации

### Из документации multisplit.md (стр. 1083):

```
SbpMemberId  Number  Да*
```

**Тип:** `Number`  
**Обязательность:** Да (для выплат по СБП)  
**Описание:** Идентификатор банка-получателя в СБП

### ⚠️ Противоречие в документации

В примерах на стр. 897-906 показано:

```json
{
  "SbpMemberId": "100000000004",  // ← Строка в примере!
  ...
}
```

Но в описании параметров (стр. 1083) указан тип **Number**.

**Решение:** Использовать Number (согласно таблице параметров, а не примерам).

---

## ✅ Решение

Преобразовать `SbpMemberId` в число перед отправкой в API:

### Файл: `src/lib/tbank.ts` (строка 346)

**Было:**

```typescript
// Если выплата по СБП - дополнительно добавляем Phone + SbpMemberId
if (params.phone && params.sbpMemberId) {
	requestBody.Phone = params.phone
	requestBody.SbpMemberId = params.sbpMemberId // ← Строка
}
```

**Стало:**

```typescript
// Если выплата по СБП - дополнительно добавляем Phone + SbpMemberId
if (params.phone && params.sbpMemberId) {
	requestBody.Phone = params.phone
	requestBody.SbpMemberId = Number(params.sbpMemberId) // ← Число
}
```

### Файл: `test-signature.js` (строка 16)

**Было:**

```javascript
const requestBody = {
	// ...
	SbpMemberId: '100000000004', // ← Строка
	// ...
}
```

**Стало:**

```javascript
const requestBody = {
	// ...
	SbpMemberId: 100000000004, // ← Число
	// ...
}
```

---

## 🔍 Почему это важно?

### Влияние на запрос к API

Когда `SbpMemberId` является строкой:

```json
{
	"SbpMemberId": "100000000004" // ← T-Bank может отклонить
}
```

Когда `SbpMemberId` является числом:

```json
{
	"SbpMemberId": 100000000004 // ← Правильный формат
}
```

### Влияние на подпись (Token)

**Важно:** При генерации подписи число преобразуется в строку функцией `String(value)`, поэтому подпись остается той же:

```
...7966276597310000000000041763372956356E2C...
```

Но T-Bank API строго проверяет **типы параметров в JSON**, поэтому важно передавать правильный тип.

---

## 🚀 Применение исправления

### 1. На локальной машине:

```powershell
cd C:\Users\Perfercher\Desktop\nesi\NESI
git add .
git commit -m "fix: изменить SbpMemberId на Number (исправление ошибки 322)"
git push origin main
```

### 2. На production сервере:

```bash
cd ~/nesi-app

# Загрузите изменения
git pull origin main

# Пересоберите проект
npm run build

# Перезапустите сервис
sudo systemctl restart nesi-app.service

# Проверьте логи
sudo journalctl -u nesi-app.service -f
```

---

## ✅ Ожидаемый результат

После применения исправления запрос к T-Bank будет выглядеть так:

**Было (с ошибкой 322):**

```json
{
	"TerminalKey": "1763372956356E2C",
	"Amount": 10000,
	"OrderId": "withdraw_...",
	"DealId": "56868517",
	"PaymentRecipientId": "79662765973",
	"Phone": "79662765973",
	"SbpMemberId": "100000000004", // ← ❌ Строка
	"FinalPayout": "true",
	"NotificationURL": "https://nesi.su/api/wallet/tbank/webhook",
	"Token": "..."
}
```

**Стало (должно работать):**

```json
{
	"TerminalKey": "1763372956356E2C",
	"Amount": 10000,
	"OrderId": "withdraw_...",
	"DealId": "56868517",
	"PaymentRecipientId": "79662765973",
	"Phone": "79662765973",
	"SbpMemberId": 100000000004, // ← ✅ Число
	"FinalPayout": "true", // ← ✅ Строка (предыдущее исправление)
	"NotificationURL": "https://nesi.su/api/wallet/tbank/webhook",
	"Token": "..."
}
```

---

## 📋 Проверка после применения

### 1. Проверьте логи при создании выплаты:

```bash
sudo journalctl -u nesi-app.service --since "1 minute ago" --no-pager | grep -E "CREATE-WITHDRAWAL|TBANK"
```

**Должно быть:**

```
📤 [TBANK] Подготовка запроса на выплату: {
  "SbpMemberId": 100000000004,  // ← Число без кавычек!
  "FinalPayout": "true",  // ← Строка с кавычками!
  ...
}

📥 [TBANK] Ответ от API: {
  success: true,
  errorCode: '0',
  paymentId: 'XXXXXXXX'
}
```

### 2. Протестируйте выплату:

1. Войдите в профиль пользователя
2. Нажмите "Вывести средства"
3. Введите сумму (минимум 100 ₽)
4. Выберите способ: СБП
5. Выберите банк: любой из списка
6. Нажмите "Вывести"

**Ожидается:**

- ✅ Выплата успешно создана
- ✅ Баланс уменьшился на сумму выплаты
- ✅ В логах нет ошибки 322
- ✅ Получен PaymentId от T-Bank

---

## 🔗 Связанные исправления

1. **ИСПРАВЛЕНИЕ_FINALPAYOUT_STRING.md** - изменение типа `FinalPayout` с boolean на string
2. **ИСПРАВЛЕНИЕ_PAYMENT_RECIPIENT_ID_V2.md** - правильное использование `PaymentRecipientId` для СБП

---

## 📚 Список правильных типов параметров

| Параметр             | Тип в коде | Тип в JSON | Пример значения             |
| -------------------- | ---------- | ---------- | --------------------------- |
| `TerminalKey`        | string     | String     | `"1763372956356E2C"`        |
| `Amount`             | number     | Number     | `10000`                     |
| `OrderId`            | string     | String     | `"withdraw_..."`            |
| `DealId`             | string     | String     | `"56868517"`                |
| `PaymentRecipientId` | string     | String     | `"79662765973"`             |
| `Phone`              | string     | String     | `"79662765973"`             |
| `SbpMemberId`        | number     | **Number** | `100000000004` ← **Число!** |
| `FinalPayout`        | string     | **String** | `"true"` ← **Строка!**      |
| `NotificationURL`    | string     | String     | `"https://..."`             |
| `Token`              | string     | String     | `"abc123..."`               |

---

**Готово!** 🎉 После применения этого исправления (вместе с предыдущим) выплаты через СБП должны заработать!
