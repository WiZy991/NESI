# 💰 Настройка комиссионной системы (0-10%)

## Как это работает

### Новая система комиссий

🎁 **Бонус для новичков:** Первые 3 завершённые задачи — **0% комиссии**!

После исчерпания бесплатных задач комиссия зависит от уровня исполнителя:

| Уровень | Комиссия |
|---------|----------|
| 1-2     | 10%      |
| 3       | 9% (-1%) |
| 4       | 8% (-2%) |
| 5       | 7% (-3%) |
| 6+      | 6% (-4%) |

Когда заказчик подтверждает выполнение задачи:

1. **Заказчик**: Средства размораживаются и списываются
2. **Исполнитель**: Получает **90-100%** от стоимости задачи (в зависимости от комиссии)
3. **Владелец платформы**: Получает **0-10%** комиссии автоматически

### Примеры

**Первые 3 задачи (новичок):**
- Задача стоит: **1000₽**
- Исполнитель получает: **1000₽** (100%)
- Комиссия платформы: **0₽**

**Уровень 1-2 (после бесплатных задач):**
- Задача стоит: **1000₽**
- Исполнитель получает: **900₽** (90%)
- Комиссия платформы: **100₽** (10%)

**Уровень 6 (максимальная скидка):**
- Задача стоит: **1000₽**
- Исполнитель получает: **940₽** (94%)
- Комиссия платформы: **60₽** (6%)

---

## 🚀 Быстрая настройка

### Шаг 1: Создайте аккаунт владельца платформы

```bash
# Войдите в консоль Prisma
npx prisma studio

# Или зарегистрируйтесь через интерфейс как обычный пользователь
# и получите свой ID
```

### Шаг 2: Получите ID пользователя

После регистрации/входа, скопируйте ID вашего аккаунта из:

- URL профиля: `/users/[ваш-id]`
- Из базы данных через Prisma Studio
- Из консоли разработчика в Network → `/api/profile`

### Шаг 3: Добавьте ID в переменные окружения

Создайте файл `.env` (если его нет) или добавьте строку:

```env
PLATFORM_OWNER_ID="clxxx...ваш-id"
```

### Шаг 4: Перезапустите сервер

```bash
npm run dev
```

✅ **Готово!** Теперь все комиссии будут автоматически начисляться на ваш счёт.

---

## 📊 Просмотр статистики

### Админ-панель

Перейдите в `/admin/finance` - там будет блок:

**💰 Комиссии платформы (0-10%)**

- За сегодня
- За неделю
- За месяц
- Всего заработано

### API Endpoint

```bash
GET /api/admin/platform-earnings
Authorization: Bearer YOUR_ADMIN_TOKEN
```

**Ответ:**

```json
{
	"platformOwner": {
		"id": "...",
		"email": "owner@example.com",
		"balance": 15234.56
	},
	"statistics": {
		"total": { "amount": 15234.56, "count": 76 },
		"monthly": { "amount": 3200.0, "count": 16 },
		"weekly": { "amount": 800.0, "count": 4 },
		"today": { "amount": 200.0, "count": 1 }
	},
	"recentTransactions": [
		{
			"id": "...",
			"amount": 100.0,
			"reason": "Комиссия платформы 10% с задачи \"...\""
		}
	]
}
```

---

## 🔧 Как работает расчёт комиссии

Комиссия рассчитывается автоматически в файле `src/lib/level/rewards.ts`:

```typescript
export async function calculateCommissionRate(executorXP: number, executorId?: string): Promise<number> {
  // Проверяем, есть ли у исполнителя бесплатные задачи (первые 3)
  if (executorId) {
    const executor = await prisma.user.findUnique({
      where: { id: executorId },
      select: { completedTasksCount: true }
    })
    
    // Если исполнитель выполнил менее 3 задач - комиссия 0%
    if (executor && executor.completedTasksCount < 3) {
      return 0
    }
  }

  const levelInfo = await getLevelFromXP(executorXP)
  const level = levelInfo.level

  // Уровень 1-2: комиссия 10% (базовая)
  if (level <= 2) {
    return 0.10
  }

  // Начиная с 3 уровня: снижение на 1% за уровень
  const reduction = Math.min(level - 2, 4) // Максимум 4% снижения (до 6%)
  const commissionRate = 0.10 - reduction * 0.01

  // Минимум 6%
  return Math.max(commissionRate, 0.06)
}
```

---

## 🛡️ Безопасность

### ⚠️ ВАЖНО:

- **НЕ** публикуйте `.env` файл в Git
- **НЕ** используйте тестовый аккаунт для продакшена
- Создайте отдельный email для владельца платформы
- Регулярно проверяйте транзакции на предмет мошенничества

### Рекомендации:

1. Используйте отдельный аккаунт только для получения комиссий
2. Дайте ему роль `admin` для доступа к статистике
3. Не используйте этот аккаунт для обычных действий
4. Храните креденшалы в безопасности

---

## 🧪 Тестирование

### Сценарий 1: Проверка нулевой комиссии (новичок)

1. Создайте нового пользователя-исполнителя
2. Создайте тестовую задачу на 1000₽
3. Назначьте нового исполнителя
4. Завершите задачу как заказчик
5. Проверьте:
   - Баланс исполнителя: +1000₽ (0% комиссии)
   - Баланс владельца: +0₽

### Сценарий 2: Проверка стандартной комиссии

1. У исполнителя должно быть ≥3 выполненных задач
2. Создайте задачу на 1000₽
3. Завершите задачу
4. Проверьте:
   - Баланс исполнителя: +900₽ (10% комиссии при уровне 1-2)
   - Баланс владельца: +100₽

### Сценарий 3: Просмотр статистики

1. Войдите как admin
2. Перейдите на `/admin/finance`
3. Убедитесь, что блок комиссий отображается
4. Проверьте последние транзакции

---

## ❓ FAQ

**Q: Что если я не укажу `PLATFORM_OWNER_ID`?**  
A: Комиссия будет вычислена, но не начислена никому. В консоли появится предупреждение.

**Q: Можно ли изменить владельца?**  
A: Да, просто измените `PLATFORM_OWNER_ID` в `.env` и перезапустите сервер.

**Q: Где хранятся комиссии?**  
A: На балансе пользователя с ID из `PLATFORM_OWNER_ID`. Можно вывести как обычный баланс.

**Q: Как вывести заработанные комиссии?**  
A: Используйте функцию вывода средств в профиле владельца через Т-Банк.

**Q: Как отслеживается количество бесплатных задач?**  
A: В базе данных есть поле `completedTasksCount` у каждого пользователя, которое автоматически обновляется при завершении задач.

**Q: Можно ли отменить начисление комиссии?**  
A: Вручную через Prisma Studio, но это не рекомендуется. Лучше создайте корректирующую транзакцию.

---

## 📝 Журнал изменений

### v2.0 (текущая версия)

- ✅ **Новая система комиссий 0-10%**
- ✅ Первые 3 задачи бесплатно (0% комиссии)
- ✅ Комиссия зависит от уровня исполнителя
- ✅ Минимальная комиссия 6% для уровня 6+
- ✅ Статистика в админ-панели
- ✅ API endpoint для получения данных
- ✅ Поддержка копеек (Decimal)
- ✅ История транзакций
- ✅ Интеграция с Т-Банком

### v1.0 (предыдущая)

- Фиксированная комиссия 20%

### Планируется:

- 🔄 Настройка комиссии через админку (без кода)
- 🔄 Графики заработка платформы
- 🔄 Экспорт статистики в CSV/Excel
- 🔄 Автоматический вывод комиссий на карту

---

## 🆘 Поддержка

Если что-то не работает:

1. Проверьте консоль на наличие ошибок
2. Убедитесь, что `PLATFORM_OWNER_ID` указан правильно
3. Проверьте, что у пользователя роль `admin`
4. Проверьте логи сервера при завершении задачи

Ошибки будут помечены в консоли:

```
⚠️ PLATFORM_OWNER_ID не настроен! Комиссия не будет начислена.
```
