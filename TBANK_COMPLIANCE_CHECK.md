# ✅ Проверка соответствия документации Т-Банка

## 📋 Сравнение с примером запроса для СБП (стр. 15-16 документации):

```json
{
"TerminalKey": "TerminalKeyE2C",
"OrderId": "testSBP 10",
"Phone": "79998887766",
"SbpMemberId": "100000000004",
"FinalPayout": "true",
"Amount": 100,
"DealId": "9043456",
"PaymentRecipientId": "79066589133",
"Token": "e24fd85c4c20e85d2eab5f65e8b2066c83970b"
}
```

## ✅ Что исправлено:

### 1. SbpMemberId
**Было:** `Number(params.sbpMemberId)` - передавался как число

**Стало:** `String(params.sbpMemberId)` - передается как строка (согласно примеру)

**Причина:** В примере запроса (стр. 15-16) `SbpMemberId` передается как строка `"100000000004"`, хотя в таблице параметров указано Number. Следуем примеру, так как это то, что реально работает.

### 2. FinalPayout для СБП
**Было:** Передавался только если указан `params.finalPayout === true` как boolean

**Стало:** Для СБП выплат ВСЕГДА передается как строка `"true"` (согласно примеру)

**Причина:** В примере запроса для СБП (стр. 15-16) `FinalPayout` передается как строка `"true"`, а не boolean. Это критично для работы СБП выплат.

### 3. DealId
**Уже правильно:** Передается как строка `String(params.dealId)` (согласно примеру)

### 4. Amount
**Уже правильно:** Передается как число (Number)

### 5. NotificationURL
**Уже правильно:** НЕ передается в запросе (согласно примерам)

## 📋 Текущий формат запроса для СБП:

```json
{
  "TerminalKey": "1763372956356E2C",
  "OrderId": "withdraw_...",
  "Phone": "79140722203",
  "SbpMemberId": "100000000004",  // СТРОКА (как в примере)
  "FinalPayout": "true",            // СТРОКА для СБП (как в примере)
  "Amount": 10000,                  // ЧИСЛО
  "DealId": "57185342",             // СТРОКА (как в примере)
  "PaymentRecipientId": "79140722203",
  "Token": "..."
}
```

## ✅ Все параметры теперь точно соответствуют примеру из документации!

**Важно:** 
- Для СБП `FinalPayout` ВСЕГДА передается как строка `"true"`
- `SbpMemberId` передается как строка (хотя в таблице указано Number)
- `DealId` передается как строка (хотя в таблице указано Number)

Следуем примеру запроса, так как это то, что реально работает в API Т-Банка.

