# ✅ Исправление выплат через СБП согласно документации Т-Банка

## 🔍 Проблема:
Ошибка 3004: "Способ СБП недоступен для магазина" - несмотря на то, что Т-Банк подтвердил, что всё подключено.

## ✅ Исправления:

### 1. FinalPayout для СБП выплат
**Было:** FinalPayout передавался только если указан флаг `finalPayout`

**Стало:** Для СБП выплат FinalPayout ВСЕГДА передается как строка `"true"` (согласно примеру запроса стр. 903)

```typescript
if (params.phone && params.sbpMemberId) {
    // Для СБП выплат ВСЕГДА передаем FinalPayout как строку "true"
    requestBody.FinalPayout = 'true'
}
```

### 2. SbpMemberId формат
**Было:** Передавался как число

**Стало:** Передается как строка (согласно примеру запроса стр. 902)

```typescript
requestBody.SbpMemberId = String(params.sbpMemberId)
```

### 3. Убран NotificationURL
**Было:** NotificationURL передавался в запросе

**Стало:** NotificationURL не передается (согласно примерам запросов стр. 896-908)

## 📋 Текущий формат запроса для СБП:

```json
{
  "TerminalKey": "1763372956356E2C",
  "OrderId": "withdraw_...",
  "Phone": "79140722203",
  "SbpMemberId": "100000000004",
  "FinalPayout": "true",
  "Amount": 10000,
  "DealId": "57185342",
  "PaymentRecipientId": "79140722203",
  "Token": "..."
}
```

**Все параметры теперь точно соответствуют примеру из документации (стр. 896-908)!**

## ⚠️ Важно:

1. **FinalPayout для СБП** - ВСЕГДА передается как строка `"true"`, даже для частичных выплат
2. **SbpMemberId** - передается как строка (хотя в таблице параметров указано Number)
3. **NotificationURL** - не передается в запросе на выплату

## 🎯 Результат:

Запрос теперь полностью соответствует примеру из документации Т-Банка. Если ошибка 3004 сохраняется, проблема может быть в:
- Настройках терминала в личном кабинете Т-Банка
- Неправильном пароле E2C (`TBANK_E2C_PASSWORD`)
- Проблемах на стороне Т-Банка (нужно уточнить в поддержке)

