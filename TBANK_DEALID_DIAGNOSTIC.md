# 🔍 Диагностика проблемы с DealId

## ❌ Ошибка: "Не найден DealId для выплаты"

Эта ошибка означает, что система не может найти DealId в транзакциях пополнения.

---

## 🔍 Что проверить

### 1. Проверьте логи сервера

При попытке вывода должны быть логи:
```
🔍 [CREATE-WITHDRAWAL] Диагностика транзакций пополнения: { ... }
```

Это покажет:
- Сколько транзакций пополнения у пользователя
- Есть ли в них DealId
- Есть ли PaymentId

### 2. Проверьте транзакции в БД

```sql
-- Проверьте последние транзакции пополнения
SELECT 
  id,
  "dealId",
  "paymentId",
  "createdAt",
  reason
FROM "Transaction"
WHERE "userId" = 'ваш_user_id'
  AND type = 'deposit'
ORDER BY "createdAt" DESC
LIMIT 5;
```

**Ожидаемый результат:**
- Должен быть хотя бы один `dealId` (не NULL)
- Должен быть `paymentId`

### 3. Проверьте вебхук

**КРИТИЧЕСКИ ВАЖНО!** Вебхук должен быть настроен в личном кабинете Т-Банка:

1. Войдите в личный кабинет Т-Банка
2. Перейдите в настройки терминала
3. Проверьте URL вебхука: `https://nesi.su/api/wallet/tbank/webhook`
4. Убедитесь, что вебхук активен

**Проверка логов вебхука:**
При пополнении должны быть логи:
```
📥 [WEBHOOK] Входящий запрос от T-Bank
💰 [WEBHOOK] Начинаем начисление: { dealId: ..., SpAccumulationId: ... }
✅ [WEBHOOK] Начисление успешно: { dealId: ... }
```

Если DealId в логах `NULL` или отсутствует, значит:
- Вебхук не приходит с DealId
- Или вебхук не настроен правильно

### 4. Проверьте создание сделки при пополнении

При создании платежа должны быть логи:
```
🔧 [TBANK] Создаем сделку с CreateDealWithType=NN и StartSpAccumulation=NN
💳 [CREATE-PAYMENT] Ответ от Т-Банка: { dealId: ..., SpAccumulationId: ... }
```

**Важно:** DealId может не быть в ответе Init, он приходит только в вебхуке!

---

## 🔧 Решения

### Решение 1: Подождать вебхук

Вебхук от Т-Банка может прийти с задержкой (1-5 минут).

**Что делать:**
1. Подождите 2-3 минуты после пополнения
2. Попробуйте вывести снова
3. Проверьте логи - должен прийти вебхук

### Решение 2: Обновить DealId вручную

Если у вас есть PaymentId, можно обновить DealId:

**Через API:**
```bash
POST /api/wallet/tbank/update-deal-ids
Authorization: Bearer <ваш_токен>
```

Или через кнопку "Обновить DealId" в интерфейсе (если есть).

### Решение 3: Проверить вебхук в Т-Банке

**КРИТИЧЕСКИ ВАЖНО!**

1. Войдите в личный кабинет Т-Банка
2. Перейдите в настройки терминала
3. Проверьте URL вебхука: `https://nesi.su/api/wallet/tbank/webhook`
4. Убедитесь, что вебхук активен
5. Проверьте, что URL доступен извне (не localhost)

**Проверка доступности вебхука:**
```bash
curl -X POST https://nesi.su/api/wallet/tbank/webhook \
  -H "Content-Type: application/json" \
  -d '{"test": true}'
```

Должен вернуть ответ (даже если ошибка подписи).

### Решение 4: Пополнить баланс заново

Если вебхук не работает:

1. Пополните баланс заново через Т-Банк
2. Убедитесь, что вебхук настроен правильно
3. Проверьте логи сервера при пополнении
4. Дождитесь вебхука (1-2 минуты)
5. Попробуйте вывести

---

## 📊 Диагностика по логам

### Если в логах видно:

**"🔍 [CREATE-WITHDRAWAL] Диагностика транзакций пополнения: { totalDeposits: 0 }"**
→ У пользователя нет транзакций пополнения
→ **Решение:** Сначала пополните баланс

**"🔍 [CREATE-WITHDRAWAL] Диагностика транзакций пополнения: { transactions: [{ dealId: null }] }"**
→ Транзакции есть, но DealId отсутствует
→ **Решение:** 
  1. Проверьте вебхук в Т-Банке
  2. Подождите несколько минут
  3. Используйте "Обновить DealId"

**"📥 [WEBHOOK] Входящий запрос от T-Bank" отсутствует**
→ Вебхук не приходит
→ **Решение:**
  1. Проверьте настройки вебхука в Т-Банке
  2. Проверьте доступность URL извне
  3. Проверьте логи сервера на наличие входящих запросов

**"💰 [WEBHOOK] Начинаем начисление: { dealId: null, SpAccumulationId: null }"**
→ Вебхук приходит, но без DealId
→ **Решение:**
  1. Проверьте, что при создании платежа передается `CreateDealWithType: 'NN'`
  2. Проверьте логи создания платежа
  3. Возможно, нужно связаться с поддержкой Т-Банка

---

## ✅ Чеклист для решения проблемы

- [ ] Проверены логи сервера при пополнении
- [ ] Проверены логи сервера при выводе
- [ ] Проверены транзакции в БД (есть ли DealId)
- [ ] Проверен вебхук в личном кабинете Т-Банка
- [ ] Проверена доступность URL вебхука извне
- [ ] Подождали 2-3 минуты после пополнения
- [ ] Попробовали обновить DealId через API
- [ ] Проверили, что при создании платежа передается `CreateDealWithType: 'NN'`

---

## 🆘 Если ничего не помогает

1. **Проверьте логи сервера полностью** - там должна быть вся информация
2. **Свяжитесь с поддержкой Т-Банка** - возможно, проблема на их стороне
3. **Проверьте документацию** - см. `multisplit.md`, `vyplaty-multisplit.md`

---

## 📝 Дополнительная информация

- **DealId обязателен** для выплат в рамках мультирасчетов
- **DealId приходит в вебхуке** как `SpAccumulationId` или `DealId`
- **DealId может не быть в ответе Init** - это нормально
- **Вебхук может прийти с задержкой** - подождите 1-5 минут

