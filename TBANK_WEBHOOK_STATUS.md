# ✅ Статус вебхука Т-Банка

## ✅ Вебхук доступен и работает!

Вы проверили вебхук командой:
```bash
curl -X POST https://nesi.su/api/wallet/tbank/webhook \
  -H "Content-Type: application/json" \
  -d '{"test": true}'
```

**Ответ:** `{"error":"Invalid signature"}`

### ✅ Это хороший знак!

Это означает:
- ✅ Вебхук доступен извне
- ✅ Роут работает правильно
- ✅ Проверка подписи работает (отклоняет неподписанные запросы)

**Ошибка "Invalid signature" - это нормально** для тестового запроса без правильной подписи Т-Банка.

---

## 🔍 Следующий шаг: Проверьте настройки вебхука в Т-Банке

Теперь нужно убедиться, что вебхук настроен в личном кабинете Т-Банка:

### 1. Войдите в личный кабинет Т-Банка

- https://business.tinkoff.ru/
- Или через Telegram бот: @tbusiness_bot

### 2. Перейдите в настройки терминала

- Найдите ваш терминал: `1763372956356`
- Откройте настройки терминала

### 3. Найдите раздел "Вебхуки" или "Уведомления"

Может называться:
- "Callback URL"
- "Webhook URL"
- "Notification URL"
- "URL для уведомлений"

### 4. Проверьте настройки:

- **URL:** `https://nesi.su/api/wallet/tbank/webhook`
- **Метод:** POST
- **Формат:** JSON
- **Статус:** Активен/Включен ✅

### 5. Если вебхука нет:

1. Нажмите "Добавить вебхук" или "Настроить уведомления"
2. Введите URL: `https://nesi.su/api/wallet/tbank/webhook`
3. Выберите метод: POST
4. Выберите формат: JSON
5. Сохраните

---

## 📊 Проверка работы вебхука

### После настройки вебхука:

1. **Пополните баланс** через Т-Банк
2. **Проверьте логи сервера** - должны быть:
   ```
   📥 [WEBHOOK] Входящий запрос от T-Bank
   📊 [WEBHOOK] Параметры платежа: { Status: 'CONFIRMED', SpAccumulationId: ..., DealId: ... }
   💰 [WEBHOOK] Начинаем начисление: { dealId: ..., SpAccumulationId: ... }
   ✅ [WEBHOOK] Начисление успешно: { dealId: ..., savedDealId: ... }
   ```

3. **Проверьте транзакции в БД:**
   ```sql
   SELECT "dealId", "paymentId", "createdAt", reason 
   FROM "Transaction" 
   WHERE type = 'deposit' 
   ORDER BY "createdAt" DESC 
   LIMIT 1;
   ```
   
   Должен быть `dealId` (не NULL)

---

## ⚠️ Если вебхук не приходит

### Возможные причины:

1. **Вебхук не настроен в Т-Банке**
   → Решение: Настройте вебхук (см. выше)

2. **Вебхук неактивен**
   → Решение: Активируйте вебхук в настройках

3. **Неправильный URL**
   → Решение: Проверьте, что URL точно `https://nesi.su/api/wallet/tbank/webhook`

4. **Firewall блокирует запросы**
   → Решение: Проверьте настройки firewall на сервере

5. **SSL сертификат недействителен**
   → Решение: Проверьте SSL сертификат для `nesi.su`

---

## 🔄 Альтернативное решение: Обновление DealId через API

Если вебхук не работает, можно попробовать обновить DealId вручную:

```bash
POST https://nesi.su/api/wallet/tbank/update-deal-ids
Authorization: Bearer <ваш_токен>
Content-Type: application/json
```

**Ответ:**
```json
{
  "success": true,
  "message": "✅ Обновлено 1 транзакций. Теперь можно попробовать вывести средства.",
  "updated": 1,
  "failed": 0,
  "total": 1
}
```

**Ограничение:** GetState может не вернуть DealId для уже подтвержденных платежей. Вебхук - это основной способ получения DealId.

---

## ✅ Чеклист

- [x] Вебхук доступен извне (проверено через curl)
- [x] Роут работает правильно
- [x] Проверка подписи работает
- [ ] Вебхук настроен в личном кабинете Т-Банка
- [ ] Вебхук активен
- [ ] URL правильный: `https://nesi.su/api/wallet/tbank/webhook`
- [ ] После оплаты приходят логи вебхука
- [ ] DealId сохраняется в транзакциях

---

## 📝 Важно помнить

- **Вебхук ОБЯЗАТЕЛЕН** для получения DealId
- **DealId приходит ТОЛЬКО в вебхуке** (не в ответе Init)
- **Вебхук может прийти с задержкой** (1-5 минут)
- **GetState может не вернуть DealId** для уже подтвержденных платежей

---

## 🆘 Если вебхук все еще не работает

1. **Свяжитесь с поддержкой Т-Банка:**
   - Email: acq_help@tbank.ru
   - Telegram: @tbusiness_bot
   - Укажите: терминал `1763372956356`, URL вебхука

2. **Проверьте логи сервера полностью** - там должна быть вся информация

3. **Проверьте документацию** - см. `multisplit.md`, `vyplaty-multisplit.md`

