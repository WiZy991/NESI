# 🚨 Быстрое решение: Деньги не поступили на баланс

## ✅ Решение за 2 минуты

### Шаг 1: Найдите PaymentId

**Вариант A: Из личного кабинета Т-Банка**

1. Войдите в личный кабинет Т-Банка
2. Перейдите в раздел "Платежи" или "Транзакции"
3. Найдите ваш платеж
4. Скопируйте **PaymentId** (ID платежа)

**Вариант B: Из логов сервера**

```bash
# Проверьте логи на наличие PaymentId
grep -i "PaymentId" logs/app.log | tail -20
```

**Вариант C: Из консоли браузера**

1. Откройте DevTools (F12)
2. Перейдите на вкладку "Application" → "Local Storage"
3. Найдите ключ `lastTBankPaymentId`

### Шаг 2: Проверьте платеж через UI

1. Откройте профиль → вкладка "Кошелёк"
2. Нажмите "🔑 Ввести PaymentId вручную"
3. Вставьте PaymentId
4. Нажмите "Проверить"
5. Средства будут начислены автоматически!

### Шаг 3: Или через API напрямую

```bash
# Замените YOUR_DOMAIN на ваш реальный домен (например, nesi.ru)
curl -X POST https://YOUR_DOMAIN/api/wallet/tbank/check-payment \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"paymentId": "YOUR_PAYMENT_ID"}'
```

## 🔍 Проверка в базе данных

После проверки платежа проверьте, что транзакция создалась:

```sql
SELECT * FROM "Transaction"
WHERE "paymentId" = 'YOUR_PAYMENT_ID'
   OR reason LIKE '%YOUR_PAYMENT_ID%'
ORDER BY "createdAt" DESC;
```

## ⚠️ Если это не помогло

1. **Проверьте логи сервера** на наличие ошибок
2. **Проверьте настройки вебхука** в Т-Банке
3. **Проверьте переменные окружения** `TBANK_PASSWORD` и `TBANK_TERMINAL_KEY`

## 📋 Чек-лист

- [ ] Найден PaymentId
- [ ] Использована функция проверки платежа
- [ ] Проверена таблица Transaction в БД
- [ ] Проверены логи сервера
- [ ] Средства начислены на баланс

---

**Важно:** После начисления средств проверьте, что вебхук настроен правильно, чтобы в будущем это происходило автоматически.
