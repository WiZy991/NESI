# 🔍 Проверка вебхука Т-Банка

## ❌ Проблема: DealId не сохраняется

Если вы видите ошибку "Не найден DealId для выплаты", это означает, что вебхук не приходит или приходит без DealId.

---

## 🔍 Шаг 1: Проверьте логи сервера

### При создании платежа должны быть логи:

```
💳 [CREATE-PAYMENT] Ответ от Т-Банка: { paymentId: ..., dealId: ... }
✅ [CREATE-PAYMENT] Платеж успешно создан: { paymentId: ..., note: 'DealId будет получен в вебхуке после оплаты' }
```

### После оплаты должны быть логи вебхука:

```
📥 [WEBHOOK] Входящий запрос от T-Bank
📊 [WEBHOOK] Параметры платежа: { Status: 'CONFIRMED', SpAccumulationId: ..., DealId: ... }
💰 [WEBHOOK] Начинаем начисление: { dealId: ..., SpAccumulationId: ... }
✅ [WEBHOOK] Начисление успешно: { dealId: ..., savedDealId: ... }
```

**Если логов вебхука НЕТ:**
→ Вебхук не приходит
→ **Проблема:** Вебхук не настроен в Т-Банке

**Если логи есть, но `dealId: null`:**
→ Вебхук приходит, но без DealId
→ **Проблема:** Сделка не была создана или вебхук не настроен правильно

---

## 🔧 Шаг 2: Проверьте вебхук в Т-Банке

### КРИТИЧЕСКИ ВАЖНО!

1. **Войдите в личный кабинет Т-Банка**
   - https://business.tinkoff.ru/ или через Telegram бот @tbusiness_bot

2. **Перейдите в настройки терминала**
   - Найдите ваш терминал: `1763372956356`

3. **Найдите раздел "Вебхуки" или "Уведомления"**
   - Может называться "Callback URL", "Webhook URL", "Notification URL"

4. **Проверьте настройки:**
   - **URL:** `https://nesi.su/api/wallet/tbank/webhook`
   - **Метод:** POST
   - **Формат:** JSON
   - **Статус:** Активен/Включен

5. **Если вебхука нет:**
   - Добавьте новый вебхук
   - URL: `https://nesi.su/api/wallet/tbank/webhook`
   - Сохраните

---

## 🧪 Шаг 3: Проверьте доступность вебхука

### Проверка через curl:

```bash
curl -X POST https://nesi.su/api/wallet/tbank/webhook \
  -H "Content-Type: application/json" \
  -d '{"test": true}'
```

**Ожидаемый результат:**
- Должен вернуть ответ (даже если ошибка подписи)
- Если ошибка 404 или таймаут → вебхук недоступен

### Проверка через браузер:

Откройте в браузере:
```
https://nesi.su/api/wallet/tbank/webhook
```

**Ожидаемый результат:**
- Должна быть ошибка метода (GET не поддерживается)
- Если 404 → роут не работает

---

## 📊 Шаг 4: Проверьте транзакции в БД

### Проверьте последние транзакции:

```sql
SELECT 
  id,
  "dealId",
  "paymentId",
  "createdAt",
  reason,
  type
FROM "Transaction"
WHERE type = 'deposit'
ORDER BY "createdAt" DESC
LIMIT 5;
```

**Ожидаемый результат:**
- Должен быть хотя бы один `dealId` (не NULL)
- Должен быть `paymentId`

**Если `dealId` = NULL:**
→ Вебхук не пришел или пришел без DealId

---

## 🔄 Шаг 5: Попробуйте обновить DealId

### Через API:

```bash
POST https://nesi.su/api/wallet/tbank/update-deal-ids
Authorization: Bearer <ваш_токен>
Content-Type: application/json
```

**Ответ:**
```json
{
  "success": true,
  "message": "✅ Обновлено 1 транзакций. Теперь можно попробовать вывести средства.",
  "updated": 1,
  "failed": 0,
  "total": 1
}
```

**Если `updated: 0`:**
→ GetState не возвращает DealId
→ Нужно дождаться вебхука

---

## ⚠️ Важные замечания

### Ошибка 422 от preidentity

Ошибка `POST https://id.tbank.ru/preidentity/api/v1/user-info 422` - это **не критично**.

Это запрос от формы оплаты Т-Банка, который пытается получить информацию о пользователе. Это не влияет на создание платежа или получение DealId.

### Таймаут cert/check

Ошибка `GET https://securepayments-nca.tinkoff.ru:6443/cert/check net::ERR_TIMED_OUT` - это также **не критично**.

Это проверка сертификата для 3DS. Не влияет на основную функциональность.

---

## ✅ Чеклист для решения проблемы

- [ ] Проверены логи сервера при создании платежа
- [ ] Проверены логи сервера после оплаты (вебхук)
- [ ] Проверен вебхук в личном кабинете Т-Банка
- [ ] Проверена доступность URL вебхука извне
- [ ] Проверены транзакции в БД (есть ли DealId)
- [ ] Попробовано обновить DealId через API
- [ ] Подождали 2-3 минуты после оплаты

---

## 🆘 Если ничего не помогает

1. **Проверьте логи сервера полностью** - там должна быть вся информация
2. **Свяжитесь с поддержкой Т-Банка** - возможно, проблема на их стороне
3. **Проверьте документацию** - см. `multisplit.md`, `vyplaty-multisplit.md`

---

## 📝 Дополнительная информация

- **DealId приходит ТОЛЬКО в вебхуке** (не в ответе Init)
- **Вебхук может прийти с задержкой** (1-5 минут)
- **Вебхук ОБЯЗАТЕЛЕН** для получения DealId
- **GetState может не вернуть DealId** для уже подтвержденных платежей

