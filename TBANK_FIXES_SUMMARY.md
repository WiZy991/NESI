# ✅ Исправления интеграции Т-Банк

## 🔧 Что исправлено

### 1. ✅ Структура запроса Init (создание платежа)

**Проблема:** Неправильная структура параметров создания сделки

**Исправление:**
- `CreateDealWithType` теперь передается **ВНЕ блока DATA** (на верхнем уровне запроса)
- `StartSpAccumulation` теперь передается **ВНУТРИ блока DATA**

**Согласно документации:**
> "параметр CreateDealWithType со значением 'NN' (вне блока DATA)"

**Код:**
```typescript
// CreateDealWithType ВНЕ DATA
requestBody.CreateDealWithType = 'NN'

// StartSpAccumulation ВНУТРИ DATA
requestBody.DATA.StartSpAccumulation = 'NN'
```

---

### 2. ✅ Улучшенное логирование

**Добавлено:**
- Детальное логирование запроса Init (включая структуру DATA)
- Детальное логирование ответа от Init (все поля)
- Детальное логирование вебхука (все поля, включая поиск DealId)
- Детальное логирование запроса на выплату
- Обработка ошибок HTTP и парсинга JSON

**Логи теперь показывают:**
- Полную структуру запроса
- Все поля ответа
- Поиск DealId во всех возможных местах
- Ошибки с детальной информацией

---

### 3. ✅ Проверка обязательных полей в вебхуке

**Добавлено:**
- Проверка наличия `PaymentId` в вебхуке
- Проверка наличия `OrderId` в вебхуке
- Критические ошибки, если поля отсутствуют

---

### 4. ✅ Функция createSpDeal

**Добавлено:**
- Альтернативный способ создания сделки через метод `createSpDeal`
- Может использоваться, если `CreateDealWithType` не работает

---

## 📊 Что проверить сейчас

### 1. Попробуйте пополнить баланс

**Проверьте логи сервера:**
```
📤 [TBANK] Отправляем запрос Init: { ... }
🔧 [TBANK] Создаем сделку: { CreateDealWithType: 'NN', DATA_StartSpAccumulation: 'NN' }
📥 [TBANK] Полный ответ от Init: { ... }
```

**Должно быть:**
- `CreateDealWithType: 'NN'` на верхнем уровне
- `DATA.StartSpAccumulation: 'NN'` внутри DATA
- Полный ответ от API со всеми полями

---

### 2. Проверьте вебхук

**После оплаты проверьте логи:**
```
📥 [WEBHOOK] Входящий запрос от T-Bank
🔍 [WEBHOOK] Поиск DealId во всех полях: { ... }
💰 [WEBHOOK] Начинаем начисление: { dealId: ..., SpAccumulationId: ... }
✅ [WEBHOOK] Начисление успешно: { dealId: ..., savedDealId: ... }
```

**Если DealId = null:**
- Проверьте, что вебхук настроен в Т-Банке
- Проверьте, что вебхук приходит (должны быть логи)
- Проверьте, что в вебхуке есть `SpAccumulationId` или `DealId`

---

### 3. Попробуйте вывести средства

**Проверьте логи:**
```
📤 [TBANK] Подготовка запроса на выплату: { dealId: ..., ... }
📤 [TBANK] Создание выплаты: { dealId: ..., ... }
📥 [TBANK] Полный ответ от e2c/v2/Init: { ... }
```

**Должно быть:**
- `dealId` указан в запросе
- Ответ от API с `PaymentId`

---

## ⚠️ Важные замечания

1. **DealId приходит ТОЛЬКО в вебхуке** - не в ответе Init
2. **Вебхук может прийти с задержкой** - подождите 1-5 минут
3. **Вебхук ОБЯЗАТЕЛЕН** - без него DealId не будет
4. **Структура запроса критична** - CreateDealWithType должен быть вне DATA

---

## 🔍 Диагностика

### Если DealId все еще не приходит:

1. **Проверьте логи создания платежа:**
   - Должна быть структура: `CreateDealWithType` вне DATA, `StartSpAccumulation` в DATA
   - Должен быть полный ответ от Init

2. **Проверьте логи вебхука:**
   - Должен прийти вебхук после оплаты
   - В вебхуке должен быть `SpAccumulationId` или `DealId`

3. **Проверьте настройки вебхука в Т-Банке:**
   - URL: `https://nesi.su/api/wallet/tbank/webhook`
   - Статус: Активен

4. **Проверьте транзакции в БД:**
   ```sql
   SELECT "dealId", "paymentId", "createdAt", reason 
   FROM "Transaction" 
   WHERE type = 'deposit' 
   ORDER BY "createdAt" DESC 
   LIMIT 1;
   ```

---

## 📝 Следующие шаги

1. Попробуйте пополнить баланс
2. Проверьте логи сервера
3. Дождитесь вебхука (1-5 минут)
4. Проверьте транзакции - должен появиться DealId
5. Попробуйте вывести средства

---

## 🆘 Если проблема сохраняется

1. **Проверьте логи сервера полностью** - там должна быть вся информация
2. **Свяжитесь с поддержкой Т-Банка** - возможно, проблема на их стороне
3. **Проверьте документацию** - см. `multisplit.md`, `vyplaty-multisplit.md`

