# 🔧 Применение миграции на сервере

## Проблема

База данных на сервере уже существует, и Prisma не знает, какие миграции применены.

## Решение

### Вариант 1: Применить SQL вручную (рекомендуется)

1. Подключитесь к базе данных на сервере:

   ```bash
   psql -h localhost -U ваш_пользователь -d nesi_db
   ```

2. Выполните SQL из файла `apply_dealid_on_server.sql`:

   ```sql
   -- Добавляем поля
   ALTER TABLE "Transaction" ADD COLUMN IF NOT EXISTS "dealId" TEXT;
   ALTER TABLE "Transaction" ADD COLUMN IF NOT EXISTS "paymentId" TEXT;

   -- Создаем индексы
   CREATE INDEX IF NOT EXISTS "Transaction_dealId_idx" ON "Transaction"("dealId");
   CREATE INDEX IF NOT EXISTS "Transaction_paymentId_idx" ON "Transaction"("paymentId");
   ```

3. Проверьте результат:
   ```sql
   SELECT column_name, data_type
   FROM information_schema.columns
   WHERE table_name = 'Transaction'
     AND column_name IN ('dealId', 'paymentId');
   ```

### Вариант 2: Использовать prisma db push на сервере

На сервере выполните:

```bash
cd ~/nesi-app
npx prisma db push
```

Это синхронизирует схему без использования миграций.

### Вариант 3: Сделать baseline миграций

Если хотите использовать `prisma migrate deploy` в будущем:

```bash
# Пометить все существующие миграции как примененные
npx prisma migrate resolve --applied add_tbank_deal_payment_ids
```

Но сначала нужно применить SQL вручную (Вариант 1).

## После применения

1. Перезапустите приложение на сервере
2. Попробуйте вывести средства
3. Система автоматически найдет или получит DealId

---

**Рекомендация:** Используйте Вариант 1 (SQL вручную) - это самый простой и безопасный способ.
