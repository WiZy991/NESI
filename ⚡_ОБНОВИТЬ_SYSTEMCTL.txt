⚡ ОБНОВИТЬ КОД НА СЕРВЕРЕ (systemctl)

Выполните эти команды на сервере:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Обновить код:
   cd ~/nesi-app
   git pull
   # Или если нет git - скопируйте файлы вручную

2. Пересобрать:
   cd NESI
   npm run build

3. Перезапустить сервис:
   sudo systemctl restart nesi
   # Или если другое имя сервиса:
   # sudo systemctl restart nesi-app
   # sudo systemctl restart nextjs

4. Проверить статус:
   sudo systemctl status nesi

5. Проверить логи:
   sudo journalctl -u nesi -n 50 --no-pager
   # Или если другое имя:
   # sudo journalctl -u nesi-app -n 50 --no-pager

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚡ ВСЕ ОДНОЙ КОМАНДОЙ:

cd ~/nesi-app/NESI && npm run build && sudo systemctl restart nesi && sleep 2 && sudo journalctl -u nesi -n 20 --no-pager

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 ПОЛЕЗНЫЕ КОМАНДЫ:

# Просмотр логов в реальном времени:
sudo journalctl -u nesi -f

# Просмотр последних 100 строк:
sudo journalctl -u nesi -n 100 --no-pager

# Поиск ошибок:
sudo journalctl -u nesi -n 200 --no-pager | grep -i error

# Проверка статуса:
sudo systemctl status nesi

# Остановка:
sudo systemctl stop nesi

# Запуск:
sudo systemctl start nesi

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ После обновления проверьте логи на наличие ошибок!

