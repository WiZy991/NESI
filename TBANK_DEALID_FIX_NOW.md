# 🚨 СРОЧНОЕ РЕШЕНИЕ: DealId не найден

## ⚡ Что делать ПРЯМО СЕЙЧАС

### Шаг 1: Проверьте логи сервера

При пополнении должны быть логи:
```
📥 [WEBHOOK] Входящий запрос от T-Bank
💰 [WEBHOOK] Начинаем начисление: { dealId: ..., SpAccumulationId: ... }
```

**Если в логах видно `dealId: null` или `SpAccumulationId: null`:**
→ Вебхук приходит, но без DealId
→ **Проблема:** Вебхук не настроен правильно или сделка не создана

**Если логов вебхука вообще нет:**
→ Вебхук не приходит
→ **Проблема:** Вебхук не настроен в Т-Банке

---

### Шаг 2: Проверьте вебхук в Т-Банке

**КРИТИЧЕСКИ ВАЖНО!**

1. Войдите в личный кабинет Т-Банка
2. Перейдите в **Настройки терминала**
3. Найдите раздел **"Вебхуки"** или **"Уведомления"**
4. Проверьте URL: `https://nesi.su/api/wallet/tbank/webhook`
5. Убедитесь, что вебхук **активен** (включен)

**Если вебхука нет или он неактивен:**
→ Добавьте/активируйте вебхук
→ URL: `https://nesi.su/api/wallet/tbank/webhook`
→ Метод: POST
→ Формат: JSON

---

### Шаг 3: Попробуйте обновить DealId

**Через API (если есть доступ):**
```bash
POST https://nesi.su/api/wallet/tbank/update-deal-ids
Authorization: Bearer <ваш_токен>
```

**Или через браузер (если есть кнопка):**
- Найдите кнопку "Обновить DealId" в интерфейсе
- Нажмите на неё
- Подождите результат

---

### Шаг 4: Проверьте создание сделки

При создании платежа должны быть логи:
```
🔧 [TBANK] Создаем сделку с CreateDealWithType=NN и StartSpAccumulation=NN
💳 [CREATE-PAYMENT] Ответ от Т-Банка: { ... }
```

**Если этих логов нет:**
→ Сделка не создается при пополнении
→ Проверьте код создания платежа

---

## 🔍 Диагностика по логам

### Сценарий 1: Вебхук не приходит

**Логи:**
- Нет `📥 [WEBHOOK] Входящий запрос от T-Bank`
- Есть только `💳 [CREATE-PAYMENT]`

**Решение:**
1. Проверьте вебхук в Т-Банке
2. Убедитесь, что URL доступен извне
3. Проверьте firewall/SSL

---

### Сценарий 2: Вебхук приходит, но без DealId

**Логи:**
```
📥 [WEBHOOK] Входящий запрос от T-Bank
💰 [WEBHOOK] Начинаем начисление: { dealId: null, SpAccumulationId: null }
⚠️⚠️⚠️ [WEBHOOK] КРИТИЧЕСКАЯ ПРОБЛЕМА: DealId не был сохранен!
```

**Решение:**
1. Проверьте, что при создании платежа передается `CreateDealWithType: 'NN'`
2. Проверьте логи создания платежа
3. Возможно, нужно связаться с поддержкой Т-Банка

---

### Сценарий 3: Вебхук приходит с DealId, но не сохраняется

**Логи:**
```
📥 [WEBHOOK] Входящий запрос от T-Bank
💰 [WEBHOOK] Начинаем начисление: { dealId: '123456', SpAccumulationId: '123456' }
✅ [WEBHOOK] Начисление успешно: { savedDealId: 'NULL' }
```

**Решение:**
1. Проверьте код вебхука - возможно, ошибка в сохранении
2. Проверьте БД - возможно, проблема с транзакцией

---

## ✅ Быстрое решение

1. **Подождите 2-3 минуты** после пополнения (вебхук может прийти с задержкой)
2. **Попробуйте вывести снова** - система автоматически попытается найти DealId
3. **Используйте API обновления DealId:**
   ```bash
   POST /api/wallet/tbank/update-deal-ids
   ```
4. **Если не помогло** - проверьте вебхук в Т-Банке

---

## 🆘 Если ничего не помогает

1. **Проверьте логи сервера полностью** - там должна быть вся информация
2. **Проверьте настройки вебхука в Т-Банке** - это самая частая причина
3. **Свяжитесь с поддержкой Т-Банка** - возможно, проблема на их стороне
4. **Пополните баланс заново** после настройки вебхука

---

## 📝 Важно помнить

- **DealId приходит ТОЛЬКО в вебхуке** (не в ответе Init)
- **Вебхук может прийти с задержкой** (1-5 минут)
- **Вебхук ОБЯЗАТЕЛЕН** для получения DealId
- **GetState может не вернуть DealId** для уже подтвержденных платежей

