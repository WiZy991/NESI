# 🚀 НАЧНИ ЗДЕСЬ: Настройка Т-Банк Мультирасчеты

## ❓ Что не работает?

**Две проблемы:**

1. ❌ Ошибка 500 при пополнении баланса
2. ❌ Ошибка "Не найден DealId" при выводе средств

---

## ✅ Решение (3 шага по 2 минуты)

### ШАГ 1: Настройте .env

Откройте файл `.env` в корне проекта `NESI`:

```bash
nano .env
```

Добавьте эти строки (замените на ваши значения от Т-Банка):

```env
TBANK_TERMINAL_KEY="ваш_terminal_key"
TBANK_PASSWORD="ваш_password"
NEXT_PUBLIC_APP_URL="https://nesi.su"
```

Сохраните (Ctrl+O, Enter, Ctrl+X).

**Где взять ключи?**

- Личный кабинет Т-Банка → Настройки терминала
- Или запросите у менеджера Т-Банка

---

### ШАГ 2: Примените миграцию БД

Выполните одну команду:

```bash
cd NESI && npx prisma migrate deploy
```

**Или вручную через SQL:**

```bash
psql $DATABASE_URL << 'EOF'
ALTER TABLE "Transaction" ADD COLUMN IF NOT EXISTS "dealId" TEXT;
ALTER TABLE "Transaction" ADD COLUMN IF NOT EXISTS "paymentId" TEXT;
CREATE INDEX IF NOT EXISTS "Transaction_dealId_idx" ON "Transaction"("dealId");
CREATE INDEX IF NOT EXISTS "Transaction_paymentId_idx" ON "Transaction"("paymentId");
EOF
```

---

### ШАГ 3: Перезапустите приложение

```bash
pm2 restart all --update-env
```

**Или для Docker:**

```bash
docker-compose restart
```

---

## 🎯 Проверка

### 1. Быстрая проверка:

```bash
# Переменные окружения
grep TBANK .env

# БД
psql $DATABASE_URL -c "SELECT column_name FROM information_schema.columns WHERE table_name = 'Transaction' AND column_name = 'dealId';"

# Вебхук
curl https://nesi.su/api/wallet/tbank/webhook
```

### 2. Автоматическая проверка:

```bash
cd NESI
node scripts/check-tbank-setup.js
```

### 3. Тестирование:

1. Откройте https://nesi.su/wallet
2. Пополните баланс на 10 ₽
3. После оплаты проверьте логи:
   ```bash
   pm2 logs | grep "dealId"
   ```
4. Попробуйте вывести средства

---

## 🔧 Настройка вебхука в Т-Банке

**Обязательно!** Без этого DealId не будет сохраняться.

1. Откройте https://business.tbank.ru/
2. Интернет-эквайринг → Настройки терминала
3. Найдите "Вебхуки" или "Уведомления"
4. Укажите URL: `https://nesi.su/api/wallet/tbank/webhook`
5. Убедитесь, что вебхук **активен** (включен)

---

## 🆘 Не работает?

### Проблема: Ошибка 500 при пополнении

**Причина:** Неверные ключи или они не загружены

**Решение:**

```bash
# Проверьте .env
cat .env | grep TBANK

# Перезапустите с обновлением переменных
pm2 restart all --update-env
```

### Проблема: DealId не сохраняется

**Причина:** Вебхук не настроен или не приходит

**Решение:**

1. Настройте вебхук в Т-Банке (см. выше)
2. Проверьте доступность:
   ```bash
   curl https://nesi.su/api/wallet/tbank/webhook
   ```
3. Проверьте логи:
   ```bash
   pm2 logs | grep WEBHOOK
   ```

---

## 📚 Подробная документация

Если нужна более детальная инструкция:

- **[БЫСТРОЕ*РЕШЕНИЕ*ПРОБЛЕМЫ.md](./БЫСТРОЕ_РЕШЕНИЕ_ПРОБЛЕМЫ.md)** - Экспресс-инструкция
- **[НАСТРОЙКА*ТБАНК*ПОЛНАЯ.md](./НАСТРОЙКА_ТБАНК_ПОЛНАЯ.md)** - Полная инструкция
- **[КОМАНДЫ*ДЛЯ*ИСПРАВЛЕНИЯ.md](./КОМАНДЫ_ДЛЯ_ИСПРАВЛЕНИЯ.md)** - Готовые команды
- **[README*ТБАНК*ИСПРАВЛЕНИЯ.md](./README_ТБАНК_ИСПРАВЛЕНИЯ.md)** - Общая сводка

---

## ✅ Чеклист (5 минут)

Отметьте выполненные:

- [ ] Создан файл `.env` с ключами Т-Банка
- [ ] Применена миграция БД
- [ ] Приложение перезапущено
- [ ] Вебхук настроен в Т-Банке
- [ ] Тестовое пополнение прошло успешно
- [ ] DealId появился в логах
- [ ] Тестовый вывод работает

---

**Время настройки:** 5-10 минут  
**Сложность:** Легко

**Поддержка Т-Банк:** acq_help@tbank.ru
